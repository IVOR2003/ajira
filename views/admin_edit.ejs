<!DOCTYPE html>
<html>
<head>
  <title>Edit Application - BRICHAS AJIRA</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Edit Application</h1>
  <form action="/admin/update/<%= application.id %>" method="POST" enctype="multipart/form-data">
    <input type="hidden" name="old_certificate" value="<%= application.certificate %>">
    <input type="hidden" name="old_cv" value="<%= application.cv %>">
    <label for="fullname">Fullname:</label><br>
    <input type="text" name="fullname" value="<%= application.fullname %>" required><br><br>
    <label for="age">Age:</label><br>
    <input type="number" name="age" value="<%= application.age %>" required><br><br>
    <label for="gender">Gender:</label><br>
    <select name="gender" required>
      <option value="Male" <% if (application.gender === 'Male') { %> selected <% } %>>Male</option>
      <option value="Female" <% if (application.gender === 'Female') { %> selected <% } %>>Female</option>
    </select><br><br>
    <label for="address">Address:</label><br>
    <input type="text" name="address" value="<%= application.address %>" required><br><br>
    <label for="education_level">Education Level:</label><br>
    <input type="text" name="education_level" value="<%= application.education_level %>" required><br><br>
    <label for="certificate">Upload Education Certificate (Optional):</label><br>
    <% if (application.certificate) { %>
      <a href="/uploads/<%= application.certificate %>" target="_blank">View Existing</a><br>
    <% } %>
    <input type="file" name="certificate"><br><br>
    <label for="cv">Upload CV (Optional):</label><br>
    <% if (application.cv) { %>
      <a href="/uploads/<%= application.cv %>" target="_blank">View Existing</a><br>
    <% } %>
    <input type="file" name="cv"><br><br>
    <label for="first_job_type">First Job Type:</label><br>
    <input type="text" name="first_job_type" value="<%= application.first_job_type %>" required><br><br>
    <label for="second_job_type">Second Job Type:</label><br>
    <input type="text" name="second_job_type" value="<%= application.second_job_type %>" required><br><br>
    <button type="submit">Update Application</button>
  </form>
  <p><a href="/admin/dashboard">Back to Dashboard</a></p>
</body>
</html>
